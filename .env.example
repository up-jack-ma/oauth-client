# Required: Your domain URL (used for OAuth callback URLs)
BASE_URL=https://your-domain.com

# Required: Random secret for JWT signing (generate with: openssl rand -hex 32)
JWT_SECRET=your-random-secret-here

# Optional
PORT=8080
GIN_MODE=release
DB_PATH=/app/data/oauth.db
